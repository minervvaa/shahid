function Appproved() {
    x = Number(document.querySelector('input').value);

    age = x > 18;
    if (age) console.log('Approved')

    time = x > 12
    if (time) console.log('Approved')


    name.length > 4 || age < 50

    let y = false

    !y && (x === Number(x))


    age > 18 || (parentApproved && thereIsComan) 









}